export const colors = {
  btnBase: "#bf2957",
  white:'white',
  primary: "#bf2957",
  
};

import {Dimensions} from 'react-native';
const dimensions = Dimensions.get('window');
export const imageHeight = Math.round(dimensions.width * 0.35);
export const imageWidth = imageHeight;

